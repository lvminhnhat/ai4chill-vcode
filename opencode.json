{
    "$schema": "https://opencode.ai/config.json",
    "mcp": {
        "gkg": {
            "type": "remote",
            "url": "http://localhost:27495/mcp",
            "enabled": true
            // "headers": {}  // local nên không cần
        }
    }
}
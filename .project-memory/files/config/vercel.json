{
  "path": "vercel.json",
  "type": "config",
  "lastModified": "2025-11-21T00:00:00Z",
  "summary": "Vercel deployment configuration (uses Next.js defaults).\nRemoved explicit outputDirectory to allow Next.js 16 to output to .next/ (commit a1dba9a).",
  "content": {
    "buildCommand": "npx prisma generate && next build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "regions": ["sin1"],
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1"
    },
    "build": {
      "env": {
        "PRISMA_GENERATE_DATAPROXY": "false"
      }
    },
    "functions": {
      "src/app/**/*.ts": { "maxDuration": 30 },
      "src/app/**/*.tsx": { "maxDuration": 30 }
    }
  },
  "notes": [
    "outputDirectory removed in commit a1dba9a — Vercel will use Next.js .next/ output",
    "Build command includes Prisma generate and Next build",
    "Framework: Next.js 16.0.3 with Turbopack"
  ]
}

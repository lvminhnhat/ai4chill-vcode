{
  "envFiles": [
    {
      "path": ".env",
      "entries": [
        {
          "key": "DATABASE_URL",
          "value": "REDACTED",
          "note": "PostgreSQL connection URL present in .env. Value intentionally redacted in memory for security."
        },
        {
          "key": "NEXTAUTH_SECRET",
          "value": "REDACTED",
          "note": "Used by NextAuth.js v5 for session signing; ensure value set in deployments"
        },
        {
          "key": "ENCRYPTION_KEY",
          "value": "REDACTED",
          "note": "Base64-encoded key used for AES-256-GCM encryption (derive key material from this secret). Do not commit."
        },
        {
          "key": "SEPAY_API_KEY",
          "value": "REDACTED",
          "note": "API key used to create Sepay payment sessions and verify webhooks"
        },
        {
          "key": "RESEND_API_KEY",
          "value": "REDACTED",
          "note": "API key for Resend email service"
        },
        {
          "key": "RESEND_FROM",
          "value": "no-reply@example.com",
          "note": "Default From address for Resend emails"
        }
      ],
      "notes": [
        "Ensure .env is not committed with plaintext credentials.",
        "CI/CD should load DATABASE_URL and service keys from secrets manager.",
        "Local developer should create .env.local with real credentials."
      ]
    }
  ]
}
